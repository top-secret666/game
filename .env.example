# Required environment variables for game-backend
# Copy this file to .env (or export these vars in your shell/container/CI).
# NEVER commit actual credentials — this file contains only placeholder values.

# PostgreSQL connection
DB_URL=jdbc:postgresql://localhost:5432/game
DB_USERNAME=<your_db_username>
DB_PASSWORD=<your_db_password>
