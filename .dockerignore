# Build artifacts
target/
*.class

# IDE & OS noise
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db

# Local env files (may contain secrets)
.env
*.env.local

# Git history – not needed inside the image
.git/
.gitignore

# Docs / reports – reduce build context size
docs/
reports/

# Test files – not shipped in the production image
src/test/
